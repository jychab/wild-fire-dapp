export enum ContentEnum {
  POST = 'Post',
}

export enum ActionCostEnum {
  DYNAMIC = 'DYNAMIC',
  FIXED = 'FIXED',
}

export enum ActionTypeEnum {
  DEFAULT = 'DEFAULT',
  REWARD = 'REWARD',
}
export enum Sentiment {
  LIKE = 'like',
  DISLIKE = 'dislike',
  SHARE = 'share',
  TRADE = 'trade',
}
